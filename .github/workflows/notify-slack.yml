name: Notify Slack

on:
  pull_request:
    types:
      - closed
      - opened
  push:
    branches:
      - develop

jobs:
  # merged_on_develop:
  #   if: github.event.pull_request.merged == true && github.base_ref == 'develop'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Send Slack Notification
  #       uses: slackapi/slack-github-action@v2.0.0
  #       with:
  #         webhook-type: incoming-webhook
  #         payload: |
  #           {
  #             "text": "<${{ github.event.pull_request.html_url }}|PR Merged> into *develop* 👀 *${{ github.event.pull_request.title }}* by *${{ github.event.pull_request.user.login }}*",
  #           }
  #       env:
  #         SLACK_WEBHOOK_URL: ${{ secrets.SLACK_TEAM_PORTEFEUILLE }}

  pr_created_on_develop:
    if: github.event.pull_request.opened == true && github.base_ref == 'develop'
    runs-on: ubuntu-latest
    steps:
      - name: Send Slack Notification
        uses: slackapi/slack-github-action@v2.0.0
        with:
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "🔎 | PR on *develop* <${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}> by *${{ github.event.pull_request.user.login }}*"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_TEAM_PORTEFEUILLE }}

  push_to_develop:
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - name: Send Slack Notification
        uses: slackapi/slack-github-action@v2.0.0
        with:
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "🔄 | Commit on *develop* <${{ github.event.repository.html_url }}/commit/${{ github.sha }}|${{ github.event.head_commit.message }}> by *${{ github.event.pusher.name }}* "
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_TEAM_PORTEFEUILLE }}
