name: primitives
description: >-
  Pure-Dart shared primitives for the Bull monorepo: the canonical Result/Failure
  pair, plus cross-cutting value types shared by workspace packages.
publish_to: "none"
version: 0.1.0

environment:
  sdk: "3.12.2"

resolution: workspace

dependencies:
  meta: ^1.17.0

dev_dependencies:
  test: ^1.26.3
  lints: ^6.0.0
