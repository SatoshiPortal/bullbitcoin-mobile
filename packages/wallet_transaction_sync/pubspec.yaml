name: wallet_transaction_sync
description: Local-source-first wallet transaction synchronization contracts.
publish_to: "none"
version: 0.1.0

environment:
  sdk: "3.12.2"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  bull_sdk:
    git:
      url: https://github.com/SatoshiPortal/bull_sdk
      ref: 88e05c9e9d2911f3dcd44b449ee97e27c73c1e51
      path: packages/bull_sdk
  crypto: ^3.0.7
  meta: ^1.17.0
  primitives:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.26.3
