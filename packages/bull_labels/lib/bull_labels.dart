/// Public API of the `bull_labels` module.
///
/// Scaffolded in the workspace-wiring step. The label facade, value types,
/// and the persistence SPI move here in the extraction step that follows;
/// for now this is an intentionally empty placeholder so the package is a
/// valid, resolvable workspace member.
library;
