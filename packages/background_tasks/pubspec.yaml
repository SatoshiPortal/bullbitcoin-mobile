name: background_tasks
description: Headless orchestration for wallet background work.
publish_to: "none"
version: 0.1.0
resolution: workspace

environment:
  sdk: "3.12.2"
  flutter: "3.44.9"

dependencies:
  flutter:
    sdk: flutter
  workmanager: ^0.10.9
  wallet_transaction_sync:
  notifications:
  bull_logger:
  crypto: ^3.0.7
  primitives:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
