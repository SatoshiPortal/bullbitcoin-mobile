/// User preference for the embedded Tor transport.
enum TorTransportMode { automatic, direct, snowflake }

/// Transport currently configured on the embedded Arti client.
enum TorTransport { direct, snowflake }
