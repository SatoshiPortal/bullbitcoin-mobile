// The fake moved into the package so that the app's tests can reach it through `package:secrets/testing.dart`. This shim keeps the package's own tests unchanged.
export 'package:secrets/testing.dart';
