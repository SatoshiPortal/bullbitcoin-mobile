import 'package:flutter/material.dart';

class ReceiveInfoSegment extends StatelessWidget {
  const ReceiveInfoSegment({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Receive Info Segment'),
    );
  }
}
