const bullnymDefaultBaseUrl = String.fromEnvironment(
  'BULLNYM_BASE_URL',
  defaultValue: 'https://api.bullpay.app',
);

const bullnymDefaultPublicBaseUrl = String.fromEnvironment(
  'BULLNYM_PUBLIC_BASE_URL',
  defaultValue: 'https://bullpay.app',
);
