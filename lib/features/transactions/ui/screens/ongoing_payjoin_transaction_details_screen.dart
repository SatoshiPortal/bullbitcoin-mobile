import 'package:flutter/material.dart';

class OngoingPayjoinTransactionDetailsScreen extends StatelessWidget {
  const OngoingPayjoinTransactionDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ongoing Payjoin Transaction')),
      body: const SafeArea(child: Column()),
    );
  }
}
