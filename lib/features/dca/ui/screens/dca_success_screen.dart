import 'package:flutter/material.dart';

class DcaSuccessScreen extends StatelessWidget {
  const DcaSuccessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DCA Success'));
  }
}
