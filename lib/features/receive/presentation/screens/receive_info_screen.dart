import 'package:flutter/material.dart';

class ReceiveInfoScreen extends StatelessWidget {
  const ReceiveInfoScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Receive Info Screen'),
    );
  }
}
